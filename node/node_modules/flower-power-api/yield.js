function generate() {
  let args = arguments;
  
  for (let i = 0; i < arguments.length; i++) {
    console.log(arguments[i]);
  }
  // yield {name: "bruno", age: "20"};
  // yield {name: "manue", age: "21"};
  // yield {name: "ilyes", age: "28"};
};

generate("sdf", "sdfsdf");
